# 📜 Правила Разработки EvPower Backend

> **Эти правила ОБЯЗАТЕЛЬНЫ для соблюдения при любых изменениях в проекте.**
>
> Последнее обновление: 2025-11-01

---

## 🎯 Основные Принципы

### 1️⃣ Целостность и Консистентность Проекта

**ПРАВИЛО:** Весь проект должен быть единым целым. Одинаковая функциональность должна быть реализована одинаково во всех местах.

#### Что это значит:

- ✅ **Единый стиль кода** - используем Python 3.11+ с type hints
- ✅ **Единообразие паттернов** - одинаковая структура endpoints, services, CRUD
- ✅ **Консистентная обработка ошибок** - везде используем try-except с структурированным логированием
- ✅ **Единая система валидации** - Pydantic модели для всех request/response
- ✅ **Согласованность именования** - snake_case для функций/переменных, PascalCase для классов

#### Примеры:

**❌ ПЛОХО:**

```python
# В одном файле
def get_client(client_id: str):
    result = db.execute(text("SELECT * FROM clients WHERE id = :id"), {"id": client_id})
    return result.fetchone()

# В другом файле
async def fetch_client_by_id(id: str):
    query = f"SELECT * FROM clients WHERE id = '{id}'"  # SQL injection!
    return db.execute(query).fetchone()
```

**✅ ХОРОШО:**

```python
# Везде одинаково через сервисный слой
class ClientService:
    @staticmethod
    def get_client(db: Session, client_id: str) -> Optional[Client]:
        """Получает клиента по ID"""
        result = db.execute(
            text("SELECT * FROM clients WHERE id = :client_id"),
            {"client_id": client_id}
        ).fetchone()

        if not result:
            return None

        return Client(**dict(result))
```

---

### 2️⃣ Анализ Перед Действием

**ПРАВИЛО:** Сначала ПОЛНОСТЬЮ разбираемся в контексте, потом действуем.

#### Обязательные шаги перед любым изменением:

1. **Понять текущую реализацию**
   - Прочитать весь связанный код
   - Понять почему это сделано именно так
   - Найти все зависимости (API → Service → CRUD → DB)

2. **Проанализировать проблему**
   - Точно определить корневую причину
   - Проверить логи (application logs, PostgreSQL logs)
   - Проверить взаимодействие с Supabase, Redis, OBANK

3. **Оценить влияние изменений**
   - Какие endpoints затронуты?
   - Какие таблицы БД изменятся?
   - Повлияет ли на мобильное приложение (breaking changes)?
   - Повлияет ли на OCPP станции?
   - Повлияет ли на платежную систему (критично!)?
   - Повлияет ли на безопасность?

4. **Спланировать решение**
   - Продумать архитектуру изменений
   - Определить необходимые тесты
   - Проверить совместимость с Supabase RLS
   - Нужна ли миграция БД?

5. **Проверить альтернативы**
   - Есть ли более простое решение?
   - Можно ли решить через PostgreSQL функции?
   - Не создаем ли мы технический долг?

#### Чек-лист перед изменением:

```
□ Прочитал весь связанный код
□ Понял почему это работает сейчас
□ Нашел все зависимости (grep/rg)
□ Проверил RLS политики в БД
□ Проверил тесты для этого модуля
□ Понял какие тесты могут сломаться
□ Оценил влияние на API контракты
□ Оценил влияние на мобильное приложение
□ Оценил влияние на OCPP станции
□ Спланировал изменения (если большие - написал план)
□ Проверил что не нарушаю другие правила
```

---

### 3️⃣ Сохранение Существующего Функционала

**ПРАВИЛО:** Не меняем существующий функционал кардинально. Только рефакторинг и улучшения.

#### Разрешено:

- ✅ Рефакторинг без изменения поведения
- ✅ Добавление новых endpoints/services
- ✅ Оптимизация производительности (если поведение остается прежним)
- ✅ Исправление багов
- ✅ Улучшение обработки ошибок
- ✅ Добавление валидации через Pydantic

#### Запрещено:

- ❌ Изменение API контрактов без обсуждения (breaking changes)
- ❌ Удаление существующих endpoints
- ❌ Изменение структуры БД без миграции
- ❌ Изменение поведения платежной системы без тщательного тестирования
- ❌ Переписывание больших блоков "с нуля" без крайней необходимости

#### Если НЕОБХОДИМО кардинальное изменение:

1. **ОСТАНОВИСЬ и СПРОСИ пользователя**
2. Объясни почему текущая реализация не подходит
3. Предложи альтернативы
4. Получи явное подтверждение
5. Создай миграцию БД (Alembic)
6. Документируй breaking changes в CHANGELOG.md

---

### 4️⃣ Системный Подход к Исправлению Ошибок

**ПРАВИЛО:** Нашел ошибку - проверь ВСЕ возможные места с такой же ошибкой. Исправляем все сразу.

#### Алгоритм:

1. **Идентифицировать паттерн ошибки**

   ```bash
   # Пример: нашли SQL injection
   rg "f\"SELECT.*{.*}\"" backend/
   rg "\.format\(" backend/ --glob "*.py"

   # Пример: нашли отсутствие валидации
   rg "@app\.(post|put|delete)" backend/ -A 5 | grep -v "Pydantic"
   ```

2. **Найти все вхождения**
   - Использовать ripgrep/grep для поиска
   - Проверить похожие файлы
   - Проверить тесты
   - Проверить SQL файлы миграций

3. **Исправить все вхождения за один раз**
   - Не оставлять "на потом"
   - Сделать один commit со всеми исправлениями
   - Назвать commit понятно: "fix: prevent SQL injection across all endpoints"

4. **Проверить что ничего не пропущено**
   - Повторить поиск после исправлений
   - Запустить тесты
   - Проверить что приложение запускается

---

### 5️⃣ Чистый Код без Костылей

**ПРАВИЛО:** Никаких костылей, дублирований функционала и кода. Код должен быть чистым и поддерживаемым.

#### Запрещено:

❌ **Дублирование кода:**

```python
# ПЛОХО - дублирование
def get_client_balance(db: Session, client_id: str):
    return db.execute(text("SELECT balance FROM clients WHERE id = :id"), {"id": client_id}).scalar()

def fetch_balance_for_client(db: Session, client_id: str):
    return db.execute(text("SELECT balance FROM clients WHERE id = :id"), {"id": client_id}).scalar()
```

❌ **Костыли вместо решения:**

```python
# ПЛОХО - игнорирование ошибки
try:
    result = await start_charging(db, session_id)
except Exception as e:
    # TODO: fix this later
    pass
```

❌ **"Временные" решения:**

```python
# ПЛОХО - временный хардкод
if station_id == "CHR-BGK-001":
    # HACK: temporary fix for problematic station
    return special_behavior()
```

#### Разрешено:

✅ **DRY (Don't Repeat Yourself) с сервисным слоем:**

```python
# ХОРОШО - переиспользуемый сервис
class BalanceService:
    @staticmethod
    def get_balance(db: Session, client_id: str) -> Decimal:
        """Получает текущий баланс клиента"""
        result = db.execute(
            text("SELECT balance FROM clients WHERE id = :client_id"),
            {"client_id": client_id}
        ).scalar()

        if result is None:
            raise ValueError(f"Client {client_id} not found")

        return Decimal(str(result))

    @staticmethod
    def change_balance(
        db: Session,
        client_id: str,
        amount: Decimal,
        transaction_type: str,
        reference_id: Optional[str] = None
    ) -> Decimal:
        """Изменяет баланс клиента через защищенную функцию"""
        result = db.execute(
            text("""
                SELECT change_balance_secure(
                    :client_id, :amount, :transaction_type, :reference_id
                )
            """),
            {
                "client_id": client_id,
                "amount": float(amount),
                "transaction_type": transaction_type,
                "reference_id": reference_id
            }
        ).scalar()

        if not result:
            raise ValueError("Failed to change balance")

        db.commit()
        return Decimal(str(result))
```

✅ **Правильная обработка ошибок:**

```python
# ХОРОШО - структурированное логирование и обработка
try:
    result = await payment_service.process_topup(db, client_id, amount)
    logger.info(
        "Payment processed successfully",
        extra={
            "client_id": client_id,
            "amount": amount,
            "invoice_id": result.invoice_id
        }
    )
    return result
except ValueError as e:
    logger.warning(f"Invalid payment request: {e}", extra={"client_id": client_id})
    raise HTTPException(status_code=400, detail=str(e))
except Exception as e:
    logger.error(
        f"Payment processing failed: {e}",
        extra={"client_id": client_id, "amount": amount},
        exc_info=True
    )
    raise HTTPException(status_code=500, detail="Payment processing failed")
```

---

### 6️⃣ Безопасность Данных

**ПРАВИЛО:** Никакого хардкода чувствительных данных. Все секреты в переменных окружения.

#### Запрещено хардкодить:

- ❌ API ключи (Supabase, OBANK, O!Dengi)
- ❌ Пароли и сертификаты
- ❌ Database URLs и connection strings
- ❌ JWT secrets
- ❌ Service role keys
- ❌ Данные банковских карт (даже в логах!)

#### Правильный подход:

**❌ ПЛОХО:**

```python
SUPABASE_URL = "https://xxx.supabase.co"
SUPABASE_SERVICE_ROLE_KEY = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
OBANK_CERT_PASSWORD = "mypassword123"
```

**✅ ХОРОШО:**

```python
# config.py - используем pydantic-settings
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    SUPABASE_URL: str
    SUPABASE_SERVICE_ROLE_KEY: str
    OBANK_CERT_PASSWORD: str
    DATABASE_URL: str

    class Config:
        env_file = ".env"
        extra = "ignore"

settings = Settings()
```

**✅ ХОРОШО (.env.production file):**

```bash
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
OBANK_CERT_PASSWORD=secure_password_from_vault
DATABASE_URL=postgresql://user:pass@host:5432/db
```

#### Проверка перед commit:

```bash
# Проверить что нет секретов в коде
rg "supabase.*key.*=" backend/ --glob "*.py"
rg "password.*=" backend/ --glob "*.py" --glob "!config.py"
rg "sk_live_|sk_test_" backend/  # API keys паттерны

# Проверить .gitignore
cat .gitignore | grep -E "\.env|\.p12|credentials"
```

---

### 7️⃣ Коммуникация и Уточнения

**ПРАВИЛО:** Не придумываем от себя. Есть вопросы - СПРАШИВАЕМ.

#### Когда ОБЯЗАТЕЛЬНО спросить:

1. **Неясные требования**
   - "Сделай лучше" - что именно лучше?
   - "Исправь это" - что именно не так?
   - "Добавь endpoint X" - какое поведение ожидается?

2. **Несколько вариантов решения**
   - Вариант A: через БД функцию (быстрее, но менее гибко)
   - Вариант B: через Python (медленнее, но проще отлаживать)
   - Какой выбрать?

3. **Потенциальные breaking changes**
   - Это изменение сломает совместимость с мобильным приложением
   - Это изменение требует миграции БД
   - Это изменение влияет на OCPP станции
   - Продолжать?

4. **Критичные операции**
   - Изменения в платежной системе
   - Изменения в балансовых операциях
   - Изменения в OCPP протоколе
   - Изменения в RLS политиках

---

## 🔧 Дополнительные Правила

### 8️⃣ Тестирование Обязательно

**ПРАВИЛО:** Любое изменение должно быть покрыто тестами.

#### Требования:

- ✅ **Unit tests** для сервисов и утилит (Pytest)
- ✅ **Integration tests** для API endpoints (TestClient)
- ✅ **Обновление тестов** при изменении существующего кода
- ✅ **Проверка coverage** - минимум 70%

#### Запуск тестов:

```bash
# Установить зависимости
pip install pytest pytest-asyncio pytest-cov httpx

# Запустить все тесты
pytest

# С coverage отчетом
pytest --cov=backend --cov-report=html

# Только конкретный модуль
pytest backend/tests/test_auth.py

# Verbose mode
pytest -v

# Watch mode (с pytest-watch)
ptw
```

#### Что тестировать:

1. **Happy path** - нормальное поведение
2. **Edge cases** - граничные случаи (баланс = 0, отрицательная сумма)
3. **Error handling** - обработка ошибок (недостаточно средств, станция offline)
4. **Security** - попытки bypass аутентификации, SQL injection
5. **OCPP messages** - все типы сообщений (BootNotification, StartTransaction, etc.)
6. **Payment flow** - полный цикл платежа от создания до зачисления

#### Пример теста:

```python
# tests/test_balance.py
import pytest
from decimal import Decimal
from backend.app.services.balance_service import BalanceService

def test_get_balance_success(db_session, test_client):
    """Тест получения баланса клиента"""
    client_id = "test-client-123"

    balance = BalanceService.get_balance(db_session, client_id)

    assert isinstance(balance, Decimal)
    assert balance >= 0

def test_change_balance_insufficient_funds(db_session):
    """Тест списания при недостаточном балансе"""
    client_id = "test-client-123"

    with pytest.raises(ValueError, match="Insufficient funds"):
        BalanceService.change_balance(
            db_session,
            client_id,
            Decimal("-1000000"),
            "charge_payment"
        )
```

---

### 9️⃣ Документирование Кода

**ПРАВИЛО:** Код должен быть понятен без объяснений. Но docstrings и комментарии обязательны для сложной логики.

#### Обязательно документировать:

**1. Все публичные функции/классы:**

```python
def calculate_session_cost(
    energy_kwh: float,
    duration_minutes: int,
    pricing: PricingResult,
    promo_code: Optional[str] = None
) -> SessionCost:
    """
    Рассчитывает полную стоимость сессии зарядки

    Args:
        energy_kwh: Потребленная энергия в кВт⋅ч
        duration_minutes: Длительность сессии в минутах
        pricing: Применимый тариф
        promo_code: Промо-код для скидки (опционально)

    Returns:
        SessionCost: Объект с детализацией стоимости

    Raises:
        ValueError: Если параметры некорректны

    Example:
        >>> pricing = PricingResult(rate_per_kwh=Decimal("13.5"), ...)
        >>> cost = calculate_session_cost(10.5, 60, pricing)
        >>> print(cost.total)
        141.75
    """
    # ...
```

**2. Сложные алгоритмы:**

```python
# Рассчитываем стоимость с учетом тарифа и времени суток:
# - Дневной тариф (6:00-22:00): base price
# - Ночной тариф (22:00-6:00): base price * 0.7
# - Выходные: дополнительная скидка 10%
if is_weekend and time_start < time(6, 0):
    discount_multiplier = Decimal("0.63")  # 0.7 * 0.9
else:
    discount_multiplier = Decimal("0.7")
```

**3. Критичный и небезопасный код:**

```python
# ⚠️ КРИТИЧНО: Эта функция изменяет баланс напрямую в БД
# Используется ТОЛЬКО через service_role для избежания RLS
# НЕ вызывать из endpoints напрямую!
def unsafe_change_balance_direct(db: Session, client_id: str, amount: Decimal):
    """UNSAFE: Изменяет баланс в обход RLS политик"""
    # ...
```

---

### 🔟 Следование Архитектуре Проекта

**ПРАВИЛО:** Соблюдай установленную архитектуру. Не изобретай новые паттерны.

#### Структура проекта:

```
backend/
├── app/
│   ├── api/                    # API Layer
│   │   ├── mobile.py           # Legacy API (deprecated)
│   │   └── v1/                 # V1 API (рекомендуется)
│   │       ├── balance/        # Балансовые операции
│   │       ├── charging/       # Управление зарядкой
│   │       ├── locations/      # Локации и станции
│   │       ├── payment/        # Платежи
│   │       └── profile.py      # Профиль клиента
│   ├── core/                   # Core Layer
│   │   ├── config.py           # Конфигурация
│   │   ├── auth_middleware.py  # Аутентификация
│   │   ├── security_middleware.py  # Security
│   │   └── logging_config.py   # Логирование
│   ├── services/               # Business Logic Layer
│   │   ├── pricing_service.py  # Тарификация
│   │   ├── obank_service.py    # OBANK интеграция
│   │   └── ...
│   ├── crud/                   # Data Access Layer
│   │   ├── ocpp_service.py     # OCPP операции
│   │   └── ...
│   ├── db/                     # Database Layer
│   │   ├── session.py          # DB sessions
│   │   ├── models/             # SQLAlchemy модели
│   │   └── base_class.py       # Базовые классы
│   ├── schemas/                # Pydantic схемы
│   │   └── ocpp.py
│   └── main.py                 # Application entry
├── ocpp_ws_server/             # OCPP WebSocket Server
│   ├── server.py
│   ├── ws_handler.py
│   └── redis_manager.py
├── tests/                      # Тесты
│   ├── test_auth.py
│   ├── test_balance.py
│   └── ...
└── requirements.txt
```

#### Правила размещения кода:

1. **API Endpoints - ТОНКИЕ**
   - Только валидация через Pydantic
   - Вызов сервисов
   - Обработка HTTP ответов
   - НЕ ДОЛЖНО БЫТЬ бизнес-логики!

2. **Services - СРЕДНИЕ**
   - Business logic
   - Orchestration между CRUD слоями
   - Error handling
   - Transaction management

3. **CRUD - DATA ACCESS**
   - Только операции с БД
   - Использование SQLAlchemy или raw SQL
   - Никакой бизнес-логики

4. **Models - DATA STRUCTURES**
   - SQLAlchemy модели (если используются)
   - Pydantic схемы для валидации

#### Пример правильного разделения:

**❌ ПЛОХО - логика в endpoint:**

```python
@app.post("/api/v1/charging/start")
async def start_charging(request: Request):
    data = await request.json()
    client_id = data["client_id"]
    station_id = data["station_id"]

    # ПЛОХО - DB операция в endpoint
    db = next(get_db())
    result = db.execute(
        text("INSERT INTO charging_sessions ...")
    )

    # ПЛОХО - бизнес-логика в endpoint
    if result:
        balance = db.execute("SELECT balance FROM clients ...").scalar()
        if balance < 100:
            return {"error": "Insufficient funds"}

    return {"success": True}
```

**✅ ХОРОШО - логика в сервисе:**

```python
# schemas/charging.py
from pydantic import BaseModel, Field

class StartChargingRequest(BaseModel):
    client_id: str = Field(..., regex=r'^[a-f0-9-]{36}$')
    station_id: str = Field(..., min_length=1, max_length=50)
    connector_id: int = Field(1, ge=1, le=10)
    limit_type: Optional[str] = Field(None, regex=r'^(none|energy|amount)$')
    limit_value: Optional[float] = Field(None, ge=0)

# services/charging_service.py
class ChargingService:
    @staticmethod
    def start_charging_session(
        db: Session,
        client_id: str,
        station_id: str,
        connector_id: int,
        limit_type: Optional[str] = None,
        limit_value: Optional[float] = None
    ) -> ChargingSession:
        """Запускает сессию зарядки с проверками"""

        # Проверка баланса
        balance = BalanceService.get_balance(db, client_id)
        if balance < Decimal("100"):
            raise ValueError("Insufficient funds")

        # Проверка доступности станции
        station = StationService.get_station(db, station_id)
        if not station.is_available:
            raise ValueError("Station is not available")

        # Создание сессии через CRUD
        session = ChargingCRUD.create_session(
            db, client_id, station_id, connector_id, limit_type, limit_value
        )

        db.commit()
        return session

# api/v1/charging/start.py
@router.post("/start", response_model=ChargingSessionResponse)
async def start_charging(
    request: StartChargingRequest,
    db: Session = Depends(get_db),
    client_id: str = Depends(get_client_id)
):
    """Запускает сессию зарядки"""
    try:
        session = ChargingService.start_charging_session(
            db,
            client_id=client_id,
            station_id=request.station_id,
            connector_id=request.connector_id,
            limit_type=request.limit_type,
            limit_value=request.limit_value
        )

        return ChargingSessionResponse.from_orm(session)

    except ValueError as e:
        raise HTTPException(status_code=400, detail=str(e))
    except Exception as e:
        logger.error(f"Failed to start charging: {e}", exc_info=True)
        raise HTTPException(status_code=500, detail="Internal server error")
```

---

### 1️⃣1️⃣ Управление Миграциями БД

**ПРАВИЛО:** Все изменения схемы БД через миграции. Никаких ручных изменений в production.

#### Система миграций:

Текущий проект использует SQL файлы для миграций Supabase. **Рекомендуется перейти на Alembic.**

**Текущий подход (SQL файлы):**

```bash
# Создать новую миграцию
vim migrations/2025_11_01_add_client_tariffs.sql

# Применить в Supabase SQL Editor
# Скопировать содержимое → Supabase Dashboard → SQL Editor → Run
```

**Рекомендуемый подход (Alembic):**

```bash
# Установить Alembic
pip install alembic

# Инициализировать
alembic init migrations

# Создать миграцию
alembic revision --autogenerate -m "Add client_tariffs table"

# Применить миграцию
alembic upgrade head

# Откатить миграцию
alembic downgrade -1
```

#### Правила миграций:

1. **Всегда создавай миграцию для**:
   - Новых таблиц
   - Изменения колонок
   - Новых индексов
   - Новых функций/триггеров
   - Изменения RLS политик

2. **Безопасные миграции**:
   - ✅ Добавление новых колонок с DEFAULT
   - ✅ Добавление новых таблиц
   - ✅ Добавление новых индексов (CONCURRENT)
   - ❌ Удаление колонок (сначала deprecate)
   - ❌ Изменение типов данных (может сломать код)

3. **Тестирование миграций**:
   ```bash
   # На тестовой БД
   alembic upgrade head
   # Проверить что приложение работает
   pytest
   # Откатить
   alembic downgrade -1
   # Проверить что откат прошел успешно
   ```

---

### 1️⃣2️⃣ Git и Commits

**ПРАВИЛО:** Чистая история коммитов. Осмысленные сообщения.

#### Формат commit message:

```
<type>: <subject>

<body (optional)>

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>
```

**Types:**

- `feat:` - новая функциональность
- `fix:` - исправление бага
- `refactor:` - рефакторинг
- `docs:` - документация
- `test:` - тесты
- `chore:` - обслуживание (зависимости, конфиги)
- `perf:` - улучшение производительности
- `security:` - исправления безопасности
- `migration:` - миграции БД

**Примеры:**

```bash
git commit -m "feat: add Pydantic validation for charging endpoints"
git commit -m "fix: prevent SQL injection in balance queries"
git commit -m "security: fix authentication bypass on GET /api/v1/* endpoints"
git commit -m "migration: add client_tariffs table with RLS policies"
```

#### Что НЕ коммитить:

```bash
# .gitignore должен содержать:
.env
.env.production
*.p12
*.pem
*.key
__pycache__/
*.pyc
.pytest_cache/
.coverage
htmlcov/
.venv/
venv/
```

---

### 1️⃣3️⃣ Окружения и Deployment

**ПРАВИЛО:** Четкое разделение окружений. Production - священная корова.

#### Окружения:

```bash
# Development (локальная разработка)
APP_ENV=development
DEBUG=true
LOG_LEVEL=DEBUG

# Testing (CI/CD)
APP_ENV=testing
DEBUG=false
LOG_LEVEL=INFO

# Production (Coolify/Docker)
APP_ENV=production
DEBUG=false
LOG_LEVEL=WARNING
```

#### Deployment чеклист:

```
PRE-DEPLOYMENT:
□ Все тесты проходят (pytest)
□ Нет критических уязвимостей (safety check)
□ Миграции БД созданы и протестированы
□ .env.production обновлен
□ CHANGELOG.md обновлен
□ Backup БД создан

DEPLOYMENT:
□ Применить миграции БД на production
□ Пересобрать Docker образ
□ Запустить с новыми env переменными
□ Проверить health endpoint
□ Проверить логи на ошибки

POST-DEPLOYMENT:
□ Smoke tests (основные endpoints работают)
□ Проверить OCPP соединения станций
□ Проверить платежную систему (тестовый платеж)
□ Мониторинг (Sentry, логи)
```

#### Rollback план:

```bash
# Если что-то пошло не так:
1. Откатить Docker образ на предыдущую версию
2. Откатить миграции БД (alembic downgrade -1)
3. Восстановить env переменные
4. Проверить health endpoint
5. Уведомить команду
```

---

### 1️⃣4️⃣ Управление Документацией

**ПРАВИЛО:** Документация ВСЕГДА актуальная. Никаких дубликатов и устаревшего контента.

#### Структура документации:

```
EvPower-Backend/
├── README.md                          # Главный обзор проекта
├── RULES.md                           # Этот файл - правила разработки
├── CHANGELOG.md                       # История изменений
├── MIGRATION_INSTRUCTIONS.md          # Инструкция миграции Supabase
├── MIGRATION_READY.md                 # Статус готовности миграции
├── API_INTEGRATION_ANSWERS.md         # Документация API
├── MOBILE_APP_INTEGRATION_DETAILED.md # Интеграция с мобильным приложением
└── docs/                              # Дополнительная документация
    ├── api/                           # API reference
    ├── architecture.md                # Архитектура системы
    └── security.md                    # Security practices
```

#### Что ОБЯЗАТЕЛЬНО:

1. **Актуальность**
   - Изменил код → обнови документацию
   - Нашел устаревшее → исправь сразу
   - Добавил endpoint → обнови API документацию

2. **Чеклисты с текущим статусом**
   - [x] Реализовано
   - [ ] В процессе
   - [ ] Запланировано

3. **Дата последнего обновления**
   ```markdown
   **Последнее обновление:** 2025-11-01
   **Версия:** 1.0.0
   **Python:** 3.11
   **FastAPI:** 0.104.0
   ```

---

### 1️⃣5️⃣ Code Review Checklist

**ПРАВИЛО:** Перед коммитом проверь этот чеклист.

```
ФУНКЦИОНАЛЬНОСТЬ:
□ Код делает то, что должен
□ Работает в development
□ Работает в production (если возможно протестировать)
□ Обработаны edge cases
□ Нет breaking changes для мобильного приложения

КАЧЕСТВО КОДА:
□ Следует правилам RULES.md
□ Нет дублирования кода
□ Нет хардкода секретов
□ Type hints корректны
□ Flake8/Black/MyPy не ругаются

БЕЗОПАСНОСТЬ:
□ Нет SQL injection
□ Нет hardcoded credentials
□ Pydantic валидация для всех inputs
□ Authentication проверена
□ RLS политики не нарушены

АРХИТЕКТУРА:
□ Соответствует структуре проекта
□ Логика в services, не в endpoints
□ CRUD операции в отдельном слое
□ Используется dependency injection

ТЕСТИРОВАНИЕ:
□ Написаны unit тесты
□ Написаны integration тесты
□ Тесты проходят (pytest)
□ Coverage > 70% для нового кода

БАЗА ДАННЫХ:
□ Миграция создана (если изменения в схеме)
□ Миграция протестирована
□ Rollback миграции возможен
□ RLS политики обновлены

ДОКУМЕНТАЦИЯ:
□ README.md обновлен (если нужно)
□ CHANGELOG.md обновлен
□ API документация обновлена
□ Docstrings для публичных функций

ПРОИЗВОДИТЕЛЬНОСТЬ:
□ Нет N+1 запросов
□ Используются индексы БД
□ Кеширование где нужно (Redis)
□ Нет лишних DB queries

GIT:
□ Commit message осмысленный
□ Не коммитнул .env или секреты
□ Не коммитнул __pycache__ или .pyc
□ .gitignore актуален

DEPLOYMENT:
□ requirements.txt обновлен
□ Docker образ собирается
□ Health check работает
□ Environment variables документированы
```

---

## 🔄 Обновление Правил

Эти правила могут дополняться по мере развития проекта.

**История изменений:**

- 2025-11-01: Адаптирован под EvPower Backend (Python/FastAPI)
- Обновлены технологии: TypeScript → Python, React → FastAPI
- Добавлены правила для backend разработки
- Добавлены правила для БД миграций и OCPP
- Адаптированы примеры кода под Python

---

## ✅ Подтверждение

**Я, как AI-ассистент, подтверждаю что:**

1. Прочитал и понял все правила
2. Буду следовать им во всех изменениях
3. Буду использовать чек-листы перед финализацией
4. Буду задавать вопросы при неясностях
5. Не буду нарушать эти правила
6. Особое внимание уделю безопасности платежной системы
7. Всегда буду учитывать влияние на мобильное приложение и OCPP станции

**Дата:** 2025-11-01

---

**Эти правила - основа качественной разработки EvPower Backend. Следуй им всегда!** 🚀
