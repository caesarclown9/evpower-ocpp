# requirements.txt
# Backend только для операций пополнения счета и зарядки
# Аутентификация происходит через FlutterFlow

# --- Core зависимости ---
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
sqlalchemy>=2.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv
httpx>=0.24.0

# --- Для OCPP WebSocket сервера ---
ocpp
websockets
redis
psycopg2-binary

# --- УДАЛЕНО: Не используются ---
# email-validator - нет валидации email
# passlib[bcrypt] - нет хеширования паролей  
# pyjwt - нет JWT токенов
# alembic - миграции делаются через SQL напрямую в Supabase
# python-multipart - нет загрузки файлов

# --- Для O!Dengi интеграции ---
cryptography>=3.4.8

# Мониторинг и системные метрики
psutil>=5.9.0

# Фоновые задачи
apscheduler>=3.10.0